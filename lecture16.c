#include <stdio.h>

void main(){
    int score;
    printf("Input score : ");
    scanf("%d",&score);
    if (score>= 80  ){
        printf("grade A");
    }else if(score>=70){
        printf("grade B");
    }else if(score >=60){
        printf("garde c");
    }else if(score>=50){
        
    }
}